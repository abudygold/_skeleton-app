import { ToControlPipe } from './to-control.pipe';

describe('ToControlPipe', () => {
  it('create an instance', () => {
    const pipe = new ToControlPipe();
    expect(pipe).toBeTruthy();
  });
});
